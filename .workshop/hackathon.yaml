name: hackathon
base: ubuntu@24.04
sdks:
  - name: vscode-remote
    channel: latest/stable
  - name: uv
    channel: latest/stable
actions:
  install: |
   uv tool install specify-cli --from git+https://github.com/github/spec-kit.git 
